spring:
  application:
    name: menu
  profiles:
    active: dev
  cloud:
    config:
#   本地配置中心
#      uri: http://localhost:8762
#      fail-fast: true
#      label: master
###  配置中心服务化
      discovery:
        enabled: true
      serviceId: configServer
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/